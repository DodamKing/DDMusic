<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.cjs2108_kdd.dao.ReviewDAO">
	<insert id="setReviewData">
		insert into review values (default, #{vo.userIdx}, #{vo.title}, #{vo.content}, default, #{vo.hostIp}, default);
	</insert>
	
	<select id="getReviewData" resultType="com.spring.cjs2108_kdd.vo.ReviewVO">
		select * from review where visible = 1 order by idx desc;
	</select>
	
</mapper>